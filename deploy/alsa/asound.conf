# /etc/asound.conf
#
# System-wide ALSA include. Defers to /etc/assistant/alsa/active.conf, which
# is a symlink managed by /usr/local/bin/assistant-audio-mode and points at
# either /etc/assistant/alsa/i2s.conf or /etc/assistant/alsa/usb.conf.

</etc/assistant/alsa/active.conf>
