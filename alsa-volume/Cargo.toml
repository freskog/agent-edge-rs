[package]
name = "alsa-volume"
version = "0.1.0"
edition = "2021"
description = "Tiny ALSA mixer volume helper (amixer wrapper) shared across agent-edge binaries"
license = "MIT"

[lib]
name = "alsa_volume"
path = "src/lib.rs"

[dependencies]
thiserror = "2.0"
log = "0.4"
